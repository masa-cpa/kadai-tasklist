<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %> &gt; <%= task.status %></li>
  <% end %>
</ul>

<li><%= link_to '新規タスクの登録', new_task_path %></li>
<li><%= link_to 'Logout', logout_path, method: :delete %></li>
